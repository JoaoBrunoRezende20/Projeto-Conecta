import 'package:flutter/material.dart';

class CategoriaBebidas extends StatelessWidget {
  const CategoriaBebidas({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text("Bebidas")),
      body: const Center(
        child: Text("Tela de Bebidas FUNCIONANDO"),
      ),
    );
  }
}
